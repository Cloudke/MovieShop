@model IEnumerable<ApplicationCore.Models.Response.GenreResponseModel>

@foreach (var genre in Model)
{
    <a class="dropdown-item" asp-controller="Movies" asp-action="genre" asp-route-id="@genre.Id"> @genre.Name </a>
}